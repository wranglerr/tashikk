<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Bujo" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Bujo">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-amber-50 text-stone-800 font-mono min-h-screen" data-controller="keyboard-shortcuts">
    <nav class="border-b border-stone-300 bg-amber-50/90 backdrop-blur-sm sticky top-0 z-10">
      <div class="max-w-2xl mx-auto px-4 py-3 flex items-center justify-between">
        <a href="/" class="text-lg font-bold tracking-tight text-stone-700 hover:text-stone-900">bujo</a>
        <div class="flex gap-4 text-sm">
          <a href="/" class="text-stone-500 hover:text-stone-800">today</a>
          <a href="<%= monthly_log_path(year: Date.today.year, month: Date.today.month) %>" class="text-stone-500 hover:text-stone-800">monthly</a>
        </div>
      </div>
    </nav>

    <main class="max-w-2xl mx-auto px-4 py-8">
      <%= yield %>
    </main>
  </body>
</html>
